module.exports = function(sequelize, DataTypes) {
  const AnimalProduce = sequelize.define("AnimalProduce", {
    
  });
  AnimalProduce.associate = function(models) {
    
  }
  return AnimalProduce;
}